spring:
  batch:
    job:
      names: job1, job2
      enabled: true
  datasource:
    url: jdbc:mysql://localhost:3306/mysql?serverTimezone=GMT%2B8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  #    schema:
  #      - classpath:scheme.sql
  #    initialization-mode: ALWAYS
  jpa:
    hibernate:
      ddl-auto: update

  main:
    allow-bean-definition-overriding: true
